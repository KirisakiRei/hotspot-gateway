# Frontend — Hotspot Gateway

Portal captive dan dashboard admin (React + Vite + Tailwind).

Dokumentasi setup lengkap ada di [README root](../README.md).

```bash
cp .env.example .env
bun install
bun run dev
```

Portal: `http://localhost:5173`  
Admin: `http://localhost:5173/admin`
